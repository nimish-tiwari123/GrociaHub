import { noData } from "../../assets"
const NoData = () => {
  return (
    <div>
        <img src={noData} alt="No Data Found" className="nodata-img m-auto d-block"/>
    </div>
  )
}

export default NoData